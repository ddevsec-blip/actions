name: using env
on: push
env:
  APP_ENV: "dev"
jobs:
  test-env:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: calling variable
        run: echo " running app in $APP_ENV"
