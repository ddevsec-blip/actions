name: test-work
on: workflow_dispatch
jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: system-info
        run: |
          echo "firstline"
          date
          hostname
          free -m
        
    
